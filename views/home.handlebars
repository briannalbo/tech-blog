<h1>this is the homepage</h1>

<div class="posts">
  {{#each posts}}
  <article class="card">
    <div>
      <h2>
        <a href="/post/{{id}}">{{title}}</a>
      </h2>
      <p>{{post_text}}</p>
      <div>
        Posted by {{user.name}} on {{format_date createdAt}}
      </div>
    </div>
    <div class="card-body">
      {{body}}
    </div>
  </article>
  {{/each}}
</div>